---
name: baseline-ui
description: DigiStream adapter for Emil Kowalski's pinned baseline-ui skill.
---

# Baseline UI

Read root `AGENTS.md` and `.agents/README.md` first. Use this for interface baseline quality: hierarchy, spacing, typography, controls, states and restraint. Run `npm run skills:sync`, then read `.agents/vendor/emil-kowalski-skills/skills/baseline-ui/SKILL.md` for the exact pinned upstream guidance. DigiStream product truth and approved references override generic advice.
